<Window x:Class="TerrariaBackup.Windows.BackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Terraria Backup: Backup"
        Width="750"
        Height="450"
        MinWidth="750"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        SnapsToDevicePixels="True"
        Loaded="Window_OnLoaded">
    
    <Window.Resources>
        <Style TargetType="ListView">
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0 0 0 5" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <TextBlock 
            Grid.Row="0"
            Grid.Column="0"
            Text="Players" />
        
        <TextBlock
            Grid.Row="0"
            Grid.Column="2"
            Text="Worlds" />
        
        <ListView Grid.Row="1"
                  Grid.Column="0"
                  ItemsSource="{Binding Path=Players}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Found players">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Path=Checked}">
                                    <TextBlock Text="{Binding Path=Name}" />
                                </CheckBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <GridSplitter
            Grid.Row="1"
            Grid.Column="1"
            Width="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch" />
        
        <ListView Grid.Row="1"
                  Grid.Column="2"
                  ItemsSource="{Binding Path=Worlds}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Found worlds">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Path=Checked}">
                                    <TextBlock Text="{Binding Path=Name}" />
                                </CheckBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Content="Backup"
            Click="BackupButton_Click" />
    </Grid>
</Window>
